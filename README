## Wordcount

A simple C utility that reads in text from the standard output and returns the word, line, and character count when EOF is reached.

#### Installation

Run `./autogen.sh`, `./configure`, `make`, and optionally `make install` to install.

